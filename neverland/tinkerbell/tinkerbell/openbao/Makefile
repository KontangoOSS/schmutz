# APPNAME

.PHONY: build test lint clean

build:
	@echo "No build target configured — override in your project"

test:
	@echo "No test target configured — override in your project"

lint:
	@echo "No lint target configured — override in your project"

clean:
	@echo "No clean target configured — override in your project"
