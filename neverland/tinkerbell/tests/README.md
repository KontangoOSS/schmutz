# Tests

Place your test files in this directory.

## Conventions

- `make test` should run the full test suite
- Tests should be runnable in CI without external dependencies
- Use your language's standard test framework

## CI

Tests run automatically via Forgejo Actions on push and pull request.
See `.forgejo/workflows/ci.yml`.
