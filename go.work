go 1.26

// Consolidated schmutz workspace — all modules live in this repo.
// agent/, enroll/, controller/, shared/ are the canonical locations.
// The sibling repos (schmutz-agent, schmutz-enroll, schmutz-shared,
// schmutz-controller-new) are being retired.

use (
	./agent
	./controller
	./enroll
	./shared
)
